apiVersion: v1
kind: Service

metadata:
  name: devops-portfolio

spec:
  selector:
    app: devops-portfolio

  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}

  type: ClusterIP
